// import obj from '@/api/constant/UserManagement'
// const { correction } = obj
// console.log('123', correction)
// export const tableform = (str) => {
//   return correction.filter(item => (item.id === str) ? item.value : '')
// }
